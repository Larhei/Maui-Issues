<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GarbageCollection.BackgroundLeak"
             Title="BackgroundLeak">
    <ScrollView>
        <Grid RowDefinitions="*,auto,auto">
            <Border Background="{StaticResource PrimaryBrush}"  StrokeThickness="0" HorizontalOptions="Start" VerticalOptions="Start" Padding="8,4">
                <Label Text="The issue seems to be the Background. When setting it to BackgroundColor the page gets finalized." />
            </Border>
            <Image Source="dotnet_bot.png" Grid.Row="1" HeightRequest="100"/>
           
        </Grid>
    </ScrollView>
    
    
</ContentPage>