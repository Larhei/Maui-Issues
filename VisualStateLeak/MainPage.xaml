<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VisualStateLeak.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Button
                x:Name="Leak"
                Text="Leak" 
                Clicked="OnLeakClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="DontLeak"
                Text="DontLeak" 
                Clicked="OnDontLeakClicked"
                HorizontalOptions="Fill" /> 
            <Button
                x:Name="Collect"
                Text="GC" 
                Clicked="OnCollectClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
